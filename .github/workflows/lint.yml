name: Lint

on:
  pull_request

jobs:
  linting:
    name: Lintings
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🏃 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x

      - name: 💩 Code Linting
        run: pnpm run lint
